server.port: 8080

spring.datasource.url: jdbc:h2:mem:vendor;MODE=PostgreSQL


spring:
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    open-in-view: false